{
  "name": "@suit-protocol/sdk",
  "version": "0.1.0",
  "description": "SUIT Protocol SDK — pluggable ZK payment privacy for Stellar",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
