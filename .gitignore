# dependencies
node_modules/
**/node_modules/

# rust build output
target/
**/target/

# js build output
dist/
**/dist/
.vite/
**/.vite/

# stellar local state
.stellar/

# os / editor
.DS_Store
Thumbs.db

# keep ZK build artifacts (proof.hex, vk.hex, verification_key.json, zkey, ptau)
# so the on-chain demo is reproducible — do NOT ignore circuits/circom/build

# never commit secrets/temp repro
.deployer_secret
*.deployer_secret

# large powers-of-tau (regenerable; not needed at runtime)
*.ptau

# local editor / preview configs (machine-specific)
.claude/
**/.claude/
